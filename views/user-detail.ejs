<!DOCTYPE html>
<html>
  <head>
    <style>
      .card {
        box-shadow: 0 4px 4px 0;
        max-width: 300px;
        margin: auto;
        text-align: center;
        font-family: arial;
      }
      .title {
        color: grey;
        font-size: 18px;
      }
      .label {
        color: grey;
        font-size: 15px;
      }
    </style>
  </head>
  <body>
    <div class="card">
      <h1><%=user.firstname %> <%=user.lastname %></h1>
      <p class="title"><%=user.introduction %></p>
      <p><span class="label">Email:</span> <%=user.email %></p>
      <p><span class="label">Phone:</span> <%=user.phone %></p>
      <p><span class="label">Experience:</span> <%=user.experience %></p>
      <p><span class="label">Achievements:</span> <%=user.achievements %></p>
    </div>
  </body>
</html>
